#include "main.h"
#include <stdio.h>

/**
 * main - prints its name followed by a new name
 * @argc: number of arguments accepted by argv
 * @argv: Array accepting arguments
 * Return: 0 always success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
